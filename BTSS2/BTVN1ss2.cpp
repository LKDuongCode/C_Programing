#include<stdio.h>
#include<math.h>
int main(){
	float dSquare= 5;
	float dRectangle=6;
	float rRectangle=4;
	float circleRadius=7;
	const float PI=3.14;
	float pSquare= dSquare*4;
	float aSquare= dSquare*dSquare;
	float pRetangle= (dRetangle+rRetangle)*2;
	float aRetangle=dRetangle*rRetangle;
	float pCircle= (circleRadius*2)*PI;
	float aCirle= (circleRadius*circleRadius)*PI;
}
